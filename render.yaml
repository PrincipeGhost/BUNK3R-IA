services:
  - type: web
    name: bunk3r-ia
    runtime: docker
    plan: starter
    envVars:
      - key: OLLAMA_URL
        value: "http://localhost:11434"
      - key: FLASK_ENV
        value: production
    disk:
      name: ollama-data
      mountPath: /root/.ollama
      sizeGB: 10
